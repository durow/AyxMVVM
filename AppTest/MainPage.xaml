<Page
    x:Class="AppTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="欢迎使用 a-Note" VerticalAlignment="Center" Grid.Row="0"/>
        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button Content="测试按钮" Width="100" Command="{Binding CmdTest}"/>
                <TextBox Text="{Binding TestText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="200"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
